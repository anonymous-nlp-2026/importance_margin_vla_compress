# LIBERO Full FT baseline seed=0

_base_: libero_baseline.yaml

model:
  train_expert_only: true
  lora:
    rank: 0

training:
  output_dir: "checkpoints/libero_full_ft_seed0"
  save_every: 10000
  seed: 0
