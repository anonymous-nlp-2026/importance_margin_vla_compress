_base_: smart_compress.yaml

model:
  pretrained: "/root/autodl-tmp/.hf_cache/lerobot/smolvla_libero"

compressor:
  num_queries: 32      # M = 32 (75% compression)

training:
  output_dir: "checkpoints/smart_compress_m32_official"
  seed: 42
