# Baseline config: SmolVLA + LoRA fine-tuning on LIBERO, no IMM.
# L_action only (flow matching MSE).

model:
  name: smolvla
  pretrained: "/root/autodl-tmp/.hf_cache/lerobot/smolvla_base"
  lora:
    rank: 32
    alpha: 64
    dropout: 0.05
    # Expert Q/V projections + state/action projection layers
    target_modules_regex: "(vlm_with_expert\\.lm_expert\\..*\\.(q|v)_proj|(state_proj|action_in_proj|action_out_proj|action_time_mlp_in|action_time_mlp_out))"

imm:
  enabled: false

dataset:
  repo_id: "lerobot/aloha_sim_insertion_scripted_image"
  features:
    state_dim: 14
    action_dim: 14
    cameras:
      - "observation.images.top"

training:
  total_steps: 30000
  batch_size: 4
  eval_batch_size: 4
  num_workers: 4
  lr: 1.0e-4
  betas: [0.9, 0.95]
  weight_decay: 1.0e-10
  warmup_steps: 1000
  decay_lr: 2.5e-6
  grad_clip: 10.0
  eval_every: 2000
  save_every: 5000
  log_every: 50
  output_dir: "checkpoints/baseline"
  seed: 42

evaluation:
  perturbations: [0.0, 0.01, 0.02, 0.05]
  pruning_ratio: 0.5
  max_eval_batches: 50
